package org.ksam.logic.monitor.configuration;

import org.ksam.logic.configuration.IKsamElementConfig;

public class KsamMonitorConfig implements IKsamElementConfig {

    // private Map<String, String> config = new HashMap<>();
    //
    // @Override
    // public Map<String, String> getElementConfig(MeConfig meConfig) {
    // this.config.put("frequency",
    // String.valueOf(meConfig.getKsamConfig().getMonitorConfig().getFrequency()));
    // this.config.put("monMechanisms",
    // meConfig.getKsamConfig().getMonitorConfig().getMonMechanisms().toString()
    // .substring(1,
    // meConfig.getKsamConfig().getMonitorConfig().getMonMechanisms().toString().length()
    // - 1));
    // this.config.put("monOperations",
    // meConfig.getKsamConfig().getMonitorConfig().getMonOperations().toString()
    // .substring(1,
    // meConfig.getKsamConfig().getMonitorConfig().getMonOperations().toString().length()
    // - 1));
    //
    // return this.config;
    // }
    //
    // @Override
    // public void setNewEntry(String key, String value) {
    // this.config.put(key, value);
    //
    // }

}
